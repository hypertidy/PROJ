
TARGET = lib$(subst gcc,,$(COMPILED_BY))$(R_ARCH)

PKG_LIBS = \
        -lproj -ltiff -lssl  -lssh2 -ltiff -lsqlite3 -lcurl

## libproj
#PKG_LIBS = -Lproj -lstaticproj -L$(RWINLIB_TIFF)/lib -ltiff -ljpeg -lz \
# -L../windows/libcurl-7.64.1/lib${R_ARCH}${CRT} -lwinhttp -lcurl -lssh2 \
# -lz -lssl -lcrypto -lgdi32 -lws2_32 -lcrypt32 -lwldap32


CXX_STD = CXX11

all: clean winlibs

clean:
	rm -f $(SHLIB) $(OBJECTS)

winlibs:
	cp -r $(R_TOOLS_SOFT)/share/proj ../inst/

.PHONY: all winlibs clean
