
PKG_LIBS =  \
 -lproj -lsqlite3 -lcurl -ltiff -lgeotiff -Lwin64

all: clean winlibs

winlibs:
	cp -r "$(R_TOOLS_SOFT)/share/proj" ../inst/


clean:
	rm -f $(SHLIB) $(OBJECTS)

.PHONY: all winlibs clean
